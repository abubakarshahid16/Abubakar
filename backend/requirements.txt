# Pinned for reproducibility. Python 3.12.
fastapi==0.120.4
uvicorn[standard]==0.38.0
pydantic==2.12.3
pydantic-settings==2.13.0
python-multipart==0.0.20

# PDF extraction
PyMuPDF==1.26.6

# Embeddings - local ONNX int8, no torch
onnxruntime==1.24.1
tokenizers==0.22.1
numpy==2.3.4

# Vector store
lancedb==0.38.0
pyarrow>=21.0.0

# System metrics for the dashboard
psutil==7.1.3

# Ollama client (plain HTTP, no SDK needed)
httpx==0.28.1

# Tests
pytest==8.4.2
pytest-asyncio==1.3.0
