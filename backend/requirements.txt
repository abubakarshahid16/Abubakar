# Pinned for reproducibility. Python 3.12.
fastapi==0.141.1
uvicorn[standard]==0.38.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.20

# Password hashing. A cffi package: it needs a matching wheel for the platform,
# so it MUST be installed while the machine still has a network. Installing it
# later on an air-gapped target is a build from source that will not happen.
# HMAC needs nothing - stdlib.
argon2-cffi==25.1.0

# PDF extraction
PyMuPDF==1.28.2

# Embeddings - local ONNX int8, no torch
onnxruntime==1.29.0
tokenizers==0.23.2
numpy==2.5.3

# Vector store
lancedb==0.38.0
pyarrow>=25.0.1

# System metrics for the dashboard
psutil==7.2.2

# Model staging. scripts/fetch_models.py imports this directly, so it is a
# DIRECT dependency even though pip was previously getting it for free as a
# transitive of tokenizers. Undeclared, a resolver change would break model
# staging on a fresh clone - and staging is the one step that needs the
# network, so it would fail during setup on a machine about to be air-gapped.
huggingface_hub==1.30.0

# Ollama client (plain HTTP, no SDK needed)
httpx==0.28.1

# Tests
pytest==8.4.2
pytest-asyncio==1.4.0

# OCR for scanned pages - RapidOCR on the SAME onnxruntime pinned above.
# No PaddlePaddle, no torch. The weights are NOT installed by pip: they are
# vendored by scripts/fetch_models.py into backend/models/ocr and addressed by
# explicit path, because an unset model_path makes RapidOCR download from
# modelscope.cn at the FIRST recognition, which fails on an air-gapped machine
# at the worst possible moment.
rapidocr==3.9.2
# rapidocr's transitive set, pinned here rather than left to the resolver -
# an unpinned opencv is a 60 MB wheel that can change under a passing suite.
opencv-python==5.0.0.93
shapely==2.1.2
pyclipper==1.4.0
pillow==12.3.0
omegaconf==2.3.1
antlr4-python3-runtime==4.9.3
colorlog==6.12.0
PyYAML==6.0.3
tqdm==4.70.0
six==1.17.0
requests==2.34.2
