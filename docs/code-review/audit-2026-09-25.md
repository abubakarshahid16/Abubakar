# Repository audit — 2026-09-25

Read-only audit of `main` at `59318e9`. Six parallel reviews: privacy/egress,
access control, honesty invariants, tests/CI, ingestion/DB/retrieval, and
hygiene/docs. The headline findings were re-checked against the code by hand
(marked ✔ below). Nothing was fixed in this pass. No live DB was opened.

**How to read it.** Severity is the failure's cost to the client, not effort.
"New" means not already in `docs/code-review/`. Client identifiers are
deliberately not reproduced here.

## Summary

| Area | New High | New Medium | New Low | Prior findings still open |
|---|---|---|---|---|
| Privacy / egress | 1 | 7 | 1 | 10 of 14 (egress-market.md) |
| Access control | 3 | 6 | 5 | 3 of 10 (access-auth) · 7 of 11 (routes-scope) |
| Honesty invariants | 4 | 5 | – | 13 of 14 (honesty-output) · 4 of 9 (frontend) |
| Ingestion / DB / retrieval | 1 | 5 | 3 | 11 verified open of 22 (ingestion) · 6 of 15 (retrieval-chat) |
| Tests / CI | 1 | 3 | 3 | 6 confirmed open (tests-backend) |
| Hygiene / docs / confidentiality | 2 | 7 | 3 | 5 checked, all open (docs-conformance) |

## Top 10 — do these first

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| 1 | High ✔ | **The Claude review routes skip the USD caps and the provider gate.** The four `/api/reviews/runs/{id}/claude/*` routes call `reader_transport.transport()` directly: they check only the two `STANDARDS_READER_*` flags, never `REASONING_PROVIDER=claude`, and never `claude_spend.ensure_affordable`. Their spend goes to `model_spend`, not the `claude_spend` ledger, so the USD 20 total never sees it. CLAUDE.md rule 1 is false for this path. | `claude_api.py:84-95` | Require `claude_available()`; wrap every call in `ensure_affordable` + `claude_spend.record`; admin-gate the routes; make `crs-draft` a POST |
| 2 | High ✔ | **A review with zero evaluated requirements returns "Approved".** `recommend_code` has no empty-findings branch, so "no requirements extracted / none applicable" falls through to "every evaluated requirement is met". Silence read as compliance. | `comparison.py:901-982` | `if not findings:` → Manual, reason "no requirement was evaluated"; mutation-proven test |
| 3 | High ✔ | **A read grant is enough to delete a document for everyone** (also `/extract`, `/chunk?force`, `/embed`, `/index-keyword`). Upload and classification are admin-gated; delete is not. | `main.py:528-665` | Admin (or write-permission) dependency; take the #177 document claim in the manual pipeline routes |
| 4 | High ✔ | **Deliverable PATCH writes before it checks scope** — an IDOR that can re-point a hidden deliverable to the caller's own document, which then passes the after-check. | `main.py:2160-2172` | `get()` + `may_read` before `update()` |
| 5 | High ✔ | **Review findings / CRS / report leak standards the caller cannot read.** Findings are scoped on the submittal only; the standard's verbatim requirement text, clause, page and filename flow to anyone granted the submittal. Breaks rule 5. | `submittal_review.py:820-837`, `review.py:474-479,546`, `main.py:3332-3337` | Also require `standard_document_id IS NULL OR IN (allowed)`; build filename map from `allowed` only |
| 6 | High | **Unverified requirements and Claude-read datasheet values drive final verdicts without a "guess" label.** `comparison.py` never reads `needs_verification`, `confirmed_by` or `extraction_method='model'`; such findings get deterministic confidence and can set the run to Rejected/Approved. | `comparison.py:640-727` | Hold at NEEDS_ENGINEER_REVIEW with a named prefix (same pattern as `METHOD_FIELD_NAME`) |
| 7 | High | **Run code goes stale after a Claude recheck** — rows say "Needs engineer review", header and CRS still say Approved. | `claude_api.py:239-265`, `claude_recheck.py:456` | Recompute and store `recommend_code` after a recheck that raised anything |
| 8 | High | **Vector-cache rebuild can segfault the server.** `_close()` closes an mmap another thread is still multiplying against; reproduced (exit 139) with numpy 2.4. During ingestion the signature changes every batch. | `vectorcache.py:229`, `search.py:256` | Version the cache file per signature and never close a map in use, or a reader/writer lock |
| 9 | High | **Client data in tracked screenshots.** ~165 PNGs in `docs/screenshots/` and `docs/ui-redesign/` show client document numbers, standard text, an equipment tag, and the confidential register's filename. The CI guard checks extensions only. | `docs/screenshots/phase-6/`, `docs/ui-redesign/*/documents-*.png` | Untrack + redact; history rewrite is an owner decision (like B27) |
| 10 | High ✔ | **The test-count floor never fires.** `_is_full_run()` returns False when `markexpr` is set, and `pytest.ini` always sets `-m "not slow"`. Proven with `RAGINTEL_MIN_TESTS=999999`. A verification that verifies nothing. | `backend/tests/conftest.py:210`, `backend/pytest.ini:4` | Ignore the `not slow` default expression in `_is_full_run` |

## Privacy and egress

| Sev | Finding | Where |
|---|---|---|
| Med | Budget check has no lock or reservation — concurrent calls each pass against the same "spent" figure | `claude_spend.py:116-128` |
| Med | Failed or timed-out calls (incl. batches past `max_wait_s`, which still bill) are never recorded | `reasoning_provider.py:420-461` |
| Med | A model missing from `PRICES` is priced at Opus 5 (the dearest *listed*); a dearer unlisted model is under-counted 2-3×. `claude_budget.PRICE_PER_MILLION` is stale (display only) | `claude_spend.py:40`, `claude_budget.py:44-48` |
| Med | Socket-containment test does not scan `smtplib`, `anthropic`, `ssl`, `asyncio`; misses import aliases; does not scan `scripts/`. `notifications.py` passes only because `smtplib` is unscanned. Allowlist has 3 modules, CLAUDE.md says 4 | `test_socket_containment.py:348-418` |
| Med | `scripts/categorize_documents.py` sends document excerpts via raw `httpx.post` with `trust_env` on — an ambient proxy variable routes them off-machine | `scripts/categorize_documents.py:133` |
| Med | SMTP: no host allowlist, STARTTLS optional; deliverable titles from the confidential register can go in plaintext to any host | `notifications.py:57-70` |
| Med | Market "approved per query" is not enforced server-side — `/api/market/search` needs no approval token from the preview | `main.py:1360-1398` |
| Low | Synthesis DEBUG log line holds the full model completion | `synthesis.py:803-806` |

Clean: `check_model_url` (IPv6, 0.0.0.0, credentials, redirects, proxies, re-checked per request); `reader_transport` refuses redirects and proxies and logs digests only; no `anthropic` SDK import; frontend has no CDN, fonts or analytics.

## Access control

| Sev | Finding | Where |
|---|---|---|
| Med | `POST /api/risks` — no identity check, `document_id` never validated; NULL-document risks are shown to everyone | `main.py:2137-2141` |
| Med | Schedule risks copy deliverable titles into `document_id=NULL` rows → visible to every user. `GET /api/risks` writes rows and sends email as a side effect | `risks.py:37,62-69` |
| Med | Claude lane not admin-gated: any reader can rewrite a run's standard selection and spend the budget | `claude_api.py:182-289` |
| Med | Admin DB explorer reads `chunks`/`pages`/`review_findings` for every document, unaudited — contradicts "admin grants no read bypass" | `main.py:3240-3260` |
| Med | `AUTH_MODE=disabled`: anonymous caller creates a persistent admin with a redeemable setup token; no `TrustedHostMiddleware` (DNS rebinding) | `admin.py:243-247` |
| Med | Upload body spooled to disk before the 401 — unauthenticated disk fill | `main.py:343-356` |
| Low | Global settings (baseline/escalation rules, summary schedule, templates) writable by any identified user | `main.py:1512-1545,2278-2308` |
| Low | Password reset does not bump `token_epoch` — a stolen token survives the reset | `admin.py:519-559` |
| Low | Page images served `max-age=86400` without `private`/`no-store` | `main.py:2977` |
| Low | `health` docstring says it is "the one unauthenticated route" — false (progress, login, reset, risks) | `main.py:252` |

Clean: constant-time HMAC, 32-byte minimum secret, no token in URLs, CORS locked to two Vite origins, classification filters intersect only, `require_document` returns 404, upload filename sanitisation and xlsx zip-bomb limits.

## Honesty invariants

| Sev | Finding | Where |
|---|---|---|
| Med | "Needs Attention" says every Manual code means "not enough was read" — derived from the code, not the cause | `main.py:1783-1785` |
| Med | CRS prints the engineer's final code under "Recommended Review Code" | `main.py:3360`, `crs_export.py:94` |
| Med | Final-code picker defaults to the machine recommendation — one Save turns a guess into the final code | `ReviewCodePanel.tsx:31-32` |
| Med | Unconfirmed machine verdicts render as solid badges; `confirmed_by` shown as a raw user id (fixed for `decided_by` only — second home) | `reviewFormat.ts:45-54`, `FindingsTable.tsx:266` |
| Med | Missing worker values render as `0` (`?? 0`) for non-admins, who now get `worker: null` | `WorkerPanel.tsx:194-205` |

Claude output: quotes and numbers are checked against the page before acceptance (datasheet, recheck, scope records). The "guess" label is lost further downstream (Top 10 #6), and Claude-selected standards are stored `included=1` immediately (`claude_selection.py:561`).

Prior `honesty-output.md`: **13 of 14 still open**, including the unconditional "verbatim" label in the PDF report (`reports.py:360`), "audit passed" over zero markers (`reports.py:319`), and the hard-coded `0` in `analysisModel.ts:336`.

## Ingestion, DB and retrieval

| Sev | Finding | Where |
|---|---|---|
| Med ✔ | Re-chunking deletes **all** the document's vectors: the orphan DELETE runs after `DELETE FROM chunks` and before the INSERT, so `NOT IN` compares against an empty set. The comment says it deletes only orphans | `chunker.py:1590-1600` |
| Med | `_finish_if_embedded` marks every job row of the document `done` (no stage filter) — the #177 clobber survives on the success path | `ingest.py:741,762` |
| Med | `extract_document` resumes from the newest job row of any stage and overwrites a recorded failure | `extract.py:148` |
| Med | Dense path rebuilds the whole corpus matrix in heap on every signature change; `_live` keeps an mmap per document forever | `vectorcache.py` |
| Med | Manual pipeline routes race the worker and move a READY document back to `chunking` | `main.py:612-665`, `extract.py:133,212` |
| Low | Concurrent asks collide on `UNIQUE(conversation_id, ordinal)` → 500 after the answer was computed; concurrent duplicate upload → uncaught IntegrityError | `chat.py:434`, `upload.py` |
| Low | Delete leaves `vector_cache/<doc>.f32`; claims not released on restart (15-min wait); `live_guard.is_live_shaped` is case-sensitive on Windows paths | `main.py:572-590`, `live_guard.py` |
| Low | Page-image renders are not atomic; a truncated PNG is served forever | `pageimage.py:62,120` |

Clean: no SQL injection (only constants or `?` interpolated), FTS5 quoting sound, no shell/pickle/eval, watcher and upload path handling safe.

Highest open prior items: retrieval **#1 Critical** ("clause 5.3.2" becomes "clause 5", `keyword.py`), retrieval #4 (RRF boost added to rerank score, `search.py:941`), ingestion #6 (crashed OCR page dropped silently, `ocr.py:229`), #5 (OCR backlog never clears, `metrics.py:397`), #8/#9 (watcher fingerprint is size+mtime).

## Tests and CI — what was actually observed

| Check | Result |
|---|---|
| Backend `pytest` (Python 3.12) | **Not a real run.** Model weights could not be fetched (proxy 403 to huggingface/modelscope). The conftest guard correctly stopped the run (exit 4). With placeholder model files: 3369 passed, **199 failed** (all traced to the empty tokenizer), 31 skipped, 18 xfailed of 3618. Needs a real run on the owner's machine or CI |
| Skips | 25 are whole "parked: feature in stash" files (`test_typo_tolerance.py`, `test_relevance_floor.py`); 3 need a local Windows path and always skip in CI; 1 parametrised over an empty list never runs |
| `ruff` (CI subset) | All checks passed |
| `ruff` (full ruleset) | 569, mostly style; dead code: duplicate `is_comprehensive` (`analysis.py:73,313`), unused PRAGMA results (`review.py:114`, `submittal_review.py:321`) |
| Frontend `tsc -b` | 0 errors |
| Frontend `vitest` (Node 22; CI uses 24) | 711 passed, **2 failed** — both bearer-header image/PDF fetch tests (`DocumentWorkflows.test.tsx`, `PageImageViewer.citation.test.tsx`). Not confirmed on Node 24 |
| Mutation registry | 569 entries; 3 stale anchors (M108, M131, M369) never apply |
| CI | `tests.yml` uses movable action tags, not SHAs (the policy `secret-scan.yml` cites); `slow` tests never run in CI; test counts in comments are stale (409 / 407 vs 3618) |

Weak tests: `test_upload_requires_identity.py:173` never grants, so "until granted" is untested; `test_comparison.py:260` tests `compare(req, None)`, not "no matching field". Prior `tests-backend.md` F1, F6, F7, F9, F12, F13 still open.

## Hygiene, confidentiality and docs

| Sev | Finding | Where |
|---|---|---|
| ✅ | No real secrets in the available history (shallow clone; CI gitleaks with full depth is authoritative). `.env.example` has empty secrets. No PDF or SQLite tracked | – |
| High | Dashboard breaks rule 10: besides the four review cards it renders a readiness panel, an EPC tile row (with `approved ?? 0`), a second card row (latency, searchable %, a duplicate "Needs attention"), up to 7 warning rows, and a ~25-stat technical section. The comment at `:150` says the panel is "the only thing this screen gained" | `DashboardViewContent.tsx:150-306` |
| Med ✔ | Synthesis logger still named after the old product, so its records never reach the configured `rag_intelligence` handler | `synthesis.py:767` vs `errors.py:97` |
| Med | Verbatim extracted passages committed in `eval/results/*.json` (256 `returned_text` rows; register filename listed) | `eval/results/` |
| Med | A client equipment tag and document-number prefix appear in code and 3 tests; use made-up fixtures | `datasheets.py`, `submittal_review.py:360`, tests |
| Med | Pre-commit hook only active after `scripts/install-git-hooks.sh`; the client-identifier scan exists only where a local list file exists, with no CI equivalent | `.githooks/` |
| Med | `pyarrow` required but never imported | `requirements.txt:23` |
| Med | PyMuPDF is AGPL-3.0 — free, but copyleft on client delivery needs an explicit decision | `requirements.txt` |
| Low | Client name in code comments and one Claude prompt | `claude_crs_comments.py:194` et al. |
| Low | `.gitignore` lacks `*.safetensors` / `*.bin` | `.gitignore` |
| Low | Honesty audit is numbered to 55 but entry 53 is missing (54 entries) | `docs/status-honesty-audit.md` |

Prior `docs-conformance.md` H3, M1, M4, M12, M16 still open.

## Retractions owed (rule 7)

These are claims the project states that the code does not bear out; each belongs in `docs/status-honesty-audit.md`:

1. CLAUDE.md rule 1 — the Claude lane is gated by `REASONING_PROVIDER=claude` and the USD caps. False for `claude_api.py`.
2. The test-count floor in `conftest.py` — never evaluated on a normal run.
3. `chunker.py` — "deletes only orphan vectors". It deletes all of them.
4. `vectorcache._close` — the BufferError comment assumes numpy prevents closing a map in use. It does not.
5. #177 — "job-row clobbering fixed". Still present on the success path.
6. `main.py:252` — "the one unauthenticated route".
7. `DashboardViewContent.tsx:150` — "the only thing this screen gained".
8. CLAUDE.md — the socket-test `smtplib` gap is attributed to audit entry 49; entry 49 does not describe it.
