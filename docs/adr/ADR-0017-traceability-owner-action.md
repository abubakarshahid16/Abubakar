# ADR-0017: Complete review traceability

The review chain ends with the actionable workflow fields: owner is resolved
from deliverable stakeholder assignments (falling back to the finding owner),
and action is the finding's required action. The API and review UI expose both
fields together with the existing evidence chain.
