import type { EvidenceItem } from "../../types/api";
export function AnalysisSources({ selectedItem, sourcesRef }: { selectedItem: EvidenceItem | null; sourcesRef: React.RefObject<HTMLDivElement | null> }) {
  return <div ref={sourcesRef} data-testid="analysis-sources-panel">{selectedItem !== null ? <section aria-label="Selected passage" className="card-3d accent-edge relative surface-floating rounded-[var(--radius-md)] border border-signal-500/40 bg-ink-850 p-4"><div className="flex flex-wrap items-baseline gap-x-2 text-xs text-slateish-400"><span className="font-medium text-slateish-200">{selectedItem.filename}</span><span>p.{selectedItem.page_start}</span>{selectedItem.section !== null && <span>&sect; {selectedItem.section}</span>}</div><blockquote className="document-quote mt-2 whitespace-pre-wrap border-l-2 border-signal-500/60 bg-ink-900 py-2 ps-4 pe-3 text-[14px] text-slateish-100">{selectedItem.exact_span}</blockquote></section> : <section className="surface-card rounded-[var(--radius-md)] border border-ink-600 bg-ink-850 p-4"><h2 className="text-xs font-semibold uppercase tracking-wide text-slateish-400">Sources</h2><p className="mt-2 text-sm text-slateish-500">Select a citation or evidence row to inspect the exact passage here.</p></section>}</div>;
}
